# xdg-desktop-portal-wlr is usually D-Bus activated; start it when a user unit exists.
exec sh -c 'systemctl --user list-unit-files xdg-desktop-portal-wlr.service >/dev/null 2>&1 && systemctl --user start xdg-desktop-portal-wlr.service || true'
