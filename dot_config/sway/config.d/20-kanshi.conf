# Automatically switch output profiles when monitors are connected/disconnected.
# Restart on Sway reload so the matching profile is applied again.
exec_always sh -c 'pkill -x kanshi 2>/dev/null || true; exec kanshi'
