# Warp the pointer into the active Herdr pane when focus returns to Kitty.
# Debounced helper; lockfile keeps a single instance across sway reload.
exec_always ~/.local/bin/herdr-warp-on-window-focus
