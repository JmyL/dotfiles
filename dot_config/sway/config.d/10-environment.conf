# Export Wayland/Sway environment, then start graphical-session.target.
# Must be one exec — Sway runs exec lines in parallel, and portals started
# before WAYLAND_DISPLAY is imported fail with "cannot open display".
exec ~/.local/bin/sway-start-session
