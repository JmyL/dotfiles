# Output profiles are managed by kanshi.
# Keep fixed Sway output commands disabled to avoid conflicts when docking,
# undocking, or using presentation/projector outputs.
#
# output eDP-1 disable
# output DP-1 enable

# Lock shortly after Sway starts on this laptop.
exec sh -c 'sleep 1; exec "$1"' sh $lock
