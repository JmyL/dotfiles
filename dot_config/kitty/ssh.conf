# Forward local TS/VE/SESSION to TS/VE hosts when those vars are set.
# Unset vars are skipped (no error). Other hosts are unchanged.

hostname ts-* ve-*
    env TS=_kitty_copy_env_var_
    env VE=_kitty_copy_env_var_
    env SESSION=_kitty_copy_env_var_
