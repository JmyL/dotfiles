#!/usr/bin/env bash
set -euo pipefail

# Always load the local Slack stay-in-browser extension (archives → messages).
ext="${HOME}/.local/share/brave-extensions/slack-stay-in-browser"
exec /usr/bin/brave-browser-stable --load-extension="${ext}" "$@"
