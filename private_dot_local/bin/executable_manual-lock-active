#!/usr/bin/env bash
set -euo pipefail

flag="${SWAY_LOCK_FLAG:-${XDG_RUNTIME_DIR:-/tmp}/manual-lock.pid}"

[[ -f "$flag" ]] || exit 1
pid="$(cat "$flag" 2>/dev/null || true)"
[[ -n "${pid:-}" ]] || exit 1
kill -0 "$pid" 2>/dev/null
